# Marketplace and Open VSX publishing setup

Prepare for distribution: complete package.json metadata (publisher, categories, keywords, repository, bugs, icon 128x128), README with screenshots, CHANGELOG, telemetry disclosure if any data is collected. Set up Microsoft Marketplace publisher account and Open VSX publisher account. Add GitHub Actions workflow that runs vsce package on push and vsce publish + ovsx publish on tag. Verified publisher via DNS TXT record where applicable. Restrict activation events to keep startup impact low.

_Implemented for item JVSC-0006-79 by the mock agent._
